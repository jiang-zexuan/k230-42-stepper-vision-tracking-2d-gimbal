# P10：F407 驱动 TFT 与触摸交互复盘

- 日期：2026-08-23
- 分支：`feature/f407-p10-dual-axis-visual-tracking`
- 阶段状态：TFT 纯色、FT6336G 触摸坐标和四区域交互已通过；双轴视觉状态机、500 ms 锁定逻辑和英文/数字状态仪表盘已加入，整机状态转换实测待补录。
- 关联问题：`docs/08-debug/ISSUE-0011-tft-color-mismatch.md`

## 这一步做了什么

已经用 F407 的 SPI1 驱动 MSP2833/MSP2834 2.8 寸 TFT，建立了红、绿、蓝纯色验证。最初实测颜色异常，随后用 Logic 2 证明 F407 实际发出的命令、DC、CS 和 RGB565 字节正确。

已经对照模块用户手册、ILI9341 数据手册和官方 STM32 示例，移植完整初始化序列。重新下载后，屏幕已经正确显示红、绿、蓝，纯色诊断通过。

## 为什么要学

屏幕能点亮不等于屏幕已经正确工作。纯色测试把问题压缩成“指定颜色能否正确显示”，可以先验证供电、复位、背光、CS、DC、SPI 和控制器初始化，再进入文字和状态界面。

## 实测结果

| 项目 | 实际结果 | 状态 |
| --- | --- | --- |
| CubeMX/HAL 配置 | SPI1、8 位、MSB First、PB3/PB4/PB5 和 TFT GPIO 正确 | 已通过 |
| SPI 实际字节 | Logic 2 已确认命令和 RGB565 数据正确 | 已通过 |
| DC 命令/数据切换 | `0x3A` 时低，`0x55` 时高 | 已通过 |
| 最小初始化纯色 | 红、绿、蓝显示为蓝、紫、黄 | 已复现 |
| `0x20` 关闭反色 | 颜色仍异常 | 假设排除 |
| 低/高字节顺序实验 | 颜色映射改变但未恢复 | 不是根因 |
| 完整 ILI9341 初始化 | 红、绿、蓝显示正确 | 已通过 |
| TFT ID 读取 | `00 00 00 0F` | 不确定，不作为型号结论 |

## 代码动作 -> 工程意图

| 代码动作 | 工程意图 | 大白话描述给 AI |
| --- | --- | --- |
| `MX_SPI1_Init()` | 建立 F407 到 TFT 的 SPI 主机 | “让 F407 按指定 SPI 时序发送屏幕字节。” |
| `Tft_WriteCommand()` | 发送无参数控制命令 | “告诉屏幕执行某个操作。” |
| `Tft_WriteCommandData()` | 发送命令及参数 | “先告诉屏幕操作编号，再给它参数。” |
| 完整 `ILI9341_Init()` | 配置电源、Gamma、接口、像素格式和显示状态 | “先把屏幕控制器初始化到这个模块需要的工作状态。” |
| `ILI9341_FillColor()` | 写满整屏 RGB565 像素 | “先只验证颜色链路，不引入文字和界面变量。” |
| Logic 2 SPI 解码 | 观察线上真实字节 | “不要只相信代码，要证明引脚上真的发了这些数据。” |

## 复习要点

1. RGB565 颜色常量是标准值：`F800` 红、`07E0` 绿、`001F` 蓝。
2. 官方 STM32 示例的 16 位像素发送顺序是高字节先发、低字节后发。
3. “SPI 波形正确”只证明 F407 发得正确，不证明屏幕控制器已经完成正确初始化。
4. `TFT ID=00 00 00 0F` 不能直接证明型号错误；读回链路的可靠性需要模块资料保证。
5. 完整初始化序列是模块适配的一部分，不是可有可无的装饰代码。
6. 纯色诊断通过后，才能进入文字显示；先显示英文数字，再加入中文点阵，最后接入视觉状态。

## 触摸交互实测

已经完成以下链路：

1. 通过 `HAL_I2C_IsDeviceReady()` 确认 FT6336G 的 I2C 地址应答。
2. 通过 `HAL_I2C_Mem_Read()` 读取 `0x02` 和 `0x03~0x06`，获得触摸点数量与第一个触摸点原始坐标。
3. 实测当前显示为 180 度旋转，原始 X/Y 坐标均需反向：`screen_x=239-raw_x`、`screen_y=319-raw_y`。
4. 已实测四区域命中：左上、右上、左下、右下均能输出正确 `zone`。
5. 已实测触摸 UI 按下反色、松手恢复。
6. 已实测按钮按下沿动作：`MODE` 切换视觉跟踪、`TAKE` 切换接管、`STOP` 停止并清除位置有效性、`INFO` 读取电机状态。

### 代码动作 -> 工程意图

| 代码动作 | 工程意图 | 大白话描述给 AI |
| --- | --- | --- |
| `HAL_I2C_IsDeviceReady()` | 验证从设备 ACK | “先确认触摸芯片在这条总线上。” |
| `HAL_I2C_Mem_Read()` | 按寄存器读取触摸数据 | “先指定要读的寄存器，再取回数据。” |
| `screen_x=239-raw_x`、`screen_y=319-raw_y` | 适配显示 180 度旋转 | “把触摸芯片方向翻转成屏幕看到的方向。” |
| `touch_last_count==0` 到 `count==1` | 只响应按下沿 | “长按不要重复执行按钮命令。” |
| `STOP` 先停定时器再释放 EN | 建立安全停止顺序 | “先停止脉冲，再让电机可以自由转。” |

## P10 双轴视觉状态机

已经把 P09 的水平跟踪扩展为双轴状态闭环。每次主循环根据接管开关、最近有效 K230 帧、目标存在、X/Y 死区、软件位置和限位方向计算一个唯一状态：`SEARCH`、`TRACKING`、`LOCKED`、`LOST` 或 `FAULT`。

已经把“目标进入死区”与“目标已经锁定”分开：进入死区先保持 `TRACKING`，连续 500 ms 没有离开死区才进入 `LOCKED`。`SEARCH`、`LOCKED`、`LOST`、`FAULT` 都停止视觉 STEP；`FAULT` 额外释放两轴 EN。状态变化只通过 USART1 输出一次，避免心跳日志刷屏。

TFT 顶部已增加状态块和三行运行数据：`X/Y` 为图像像素误差，`P/T` 为水平/俯仰软件脉冲位置，`V/W` 为两轴速度目标。已改为局部刷新：标题只在启动时绘制，状态和三行固定宽度字段每 500 ms 覆盖更新，不再清空整块顶部区域。每一行文字由一笔 SPI 写入，避免屏幕先黑后亮造成闪烁。底部原有四个触摸按钮继续保留，方便在同一页执行模式切换、接管、停止和状态查询。

### 状态转换速查

| 条件 | 状态 | 运动动作 |
| --- | --- | --- |
| 未接管、关闭视觉、无目标帧 | `SEARCH` | 停止两轴 |
| 有目标且任一误差超出 40 px 死区 | `TRACKING` | 按 X/Y 比例速度跟随 |
| X/Y 同时在死区内持续 500 ms | `LOCKED` | 停止两轴并显示绿色 |
| 300 ms 无有效帧 | `LOST` | 停止两轴并显示橙色 |
| 未回中、未归零或下一脉冲越界 | `FAULT` | 停止两轴、释放 EN、显示红色 |

本轮代码已具备实物验收条件，`SEARCH/TRACKING/LOCKED/LOST/FAULT` 的现场日志和误差 5% 指标仍待实测，不提前写成通过。

## 已问知识点与答案

- 为什么先轮询再中断：轮询便于先确认寄存器和坐标链路；正式版本再让 `CTP_INT` 只置位事件标志，I2C 读取仍放在主循环。
- 轮询是否浪费 MCU：当前 50 ms 一次、每次读取几个字节，对 F407 负载很小；中断化是后续降低空闲总线访问和提升响应的优化。
- 为什么 `chip_id=00` 不能直接判故障：`0xA0` 的默认值可能为 0，且当前识别寄存器读回值与资料默认值不一致，只能作为待验证诊断，不作为型号结论。

## 复习要点（P10 加速批次）

1. 状态机的价值是把“现在发生了什么”和“允许做什么”统一起来；同一状态同时驱动电机安全动作、屏幕颜色和串口解释。
2. `LOCKED` 不是一次误差为零，而是误差连续满足条件并经过时间确认，避免目标在边界抖动时误报锁定。
3. `LOST` 表示通信超时，`SEARCH` 表示通信仍在工作但暂时没有目标，两者都停机但原因不同。

## 代码动作 -> 工程意图（P10 加速批次）

| 代码动作 | 工程意图 | 大白话描述给 AI |
| --- | --- | --- |
| `VisualState_Update()` | 用统一规则选择唯一视觉状态 | “请把目标、通信、接管、限位和零点条件整理成明确状态机。” |
| `visual_lock_candidate_tick` | 对中心误差做 500 ms 时间确认 | “进入中心后不要马上说锁定，保持半秒再确认。” |
| `VisualState_Set()` | 状态变化只记录一次并通知界面 | “状态改变时打一次原因日志，并让屏幕更新。” |
| `VisualUi_Refresh()` | 每 500 ms 局部覆盖动态字段 | “固定界面不要重画，只更新变动的数字和状态。” |
| `ILI9341_DrawText()` | 将完整一行字模批量写入 SPI | “不要一个像素一笔 SPI 命令，先在内存拼好一行再一次性送屏幕。” |
| `VisualState_AtLimitFault()` | 把向软件边界外的请求升级为故障 | “如果下一步会越界，停止并进入红色故障状态。” |

## 资料依据

- `D:\datasheet-knowledge\parsed\2.8inch_SPI_Module_MSP2833_MSP2834_User_Manual_CN\full.md` 第 3.1、3.2 节
- `D:\datasheet-knowledge\parsed\MSP2833_MSP2834_Specification_CN_V1.0\full.md`
- `D:\datasheet-knowledge\parsed\2.8inch_SPI_ILI9341_Demo_STM32\full.md` 的 `LCD_Init()` 与 `Lcd_WriteData_16Bit()`
- `D:\datasheet-knowledge\parsed\4-Driver_IC_Data_Sheet__ILI9341_Datasheet_part1\full.md` 第 8.2 节

## ES8388 板载命中特效音（2026-08-24）

本次在 P10 的显示与状态机基础上加入 F407 板载 ES8388 音频链路。依据探索者 V3 硬件参考手册第 2.1.28 节，使用 I2C1 配置 ES8388，使用 I2S2 + DMA 向板载扬声器发送 48 kHz、16 bit PCM。K230 仍只负责视觉结果，语音由 F407 本地播放。

### 已完成

1. 已确认 `AUDIO ES8388 probe=ok`、`AUDIO ES8388 init=ok`，板载扬声器可以发声。
2. 已将命中特效音 `hit.wav` 转为 16 bit、48 kHz、单声道 PCM 数组，并纳入 Keil 工程。
3. 已实现单声道到 I2S 双声道复制，以及每次 1024 个采样帧的 DMA 分块播放，避免 `HAL_I2S_Transmit_DMA()` 的 `uint16_t Size` 上限问题。
4. 已移除搜索、跟踪、丢失、故障等状态语音；只有状态机从其他状态进入 `LOCKED`（命中确认）时播放一次 `hit.wav`，冷却时间为 500 ms。
5. 已移除上电时的自动语音播放，避免启动时产生刺耳的预热声；I2S 仍保留约 170 ms 的静音预热，用于稳定播放起始时序。
6. 当前 Keil 构建结果为 `0 Error(s), 0 Warning(s)`。

### 代码动作 -> 工程意图

| 代码动作 | 工程意图 | 大白话描述给 AI |
| --- | --- | --- |
| `Es8388_InitPlayback()` | 建立 I2C 配置的 DAC 播放路径 | “请按芯片寄存器顺序打开 F407 到板载扬声器的播放链路。” |
| `AudioPlayer_FillSilence()` | 在 DMA 开始时先发送稳定时钟 | “语音开始前先给音频芯片一点稳定时间，但不要让用户听到提示音。” |
| `AudioPlayer_FillBuffer()` | 将单声道 PCM 复制为左右声道 | “一个采样同时送左右声道，让单声道 WAV 适配当前 I2S 双声道格式。” |
| `HAL_I2S_TxCpltCallback()` | 每个 DMA 块完成后续发下一块 | “不要一次把整段语音塞给 DMA，分块连续播放。” |
| `VisualState_PlayHitAudio()` | 将 `LOCKED` 状态变化映射到命中特效音 | “只有目标命中确认时播放一次特效音，其他状态保持静音。” |

### 本次复盘

- 电脑播放原始 WAV 清晰，开发板板载扬声器的听感不作为音频精度结论；当前先删除状态语音，保留单一命中特效音。
- 之前的低音量校准音实测刺耳，已删除；当前不在启动阶段播放任何音效。

## 视觉跟踪 PID 扩展（2026-08-24）

已经把原来的“误差乘比例增益”升级为每个轴独立的离散 PID 控制器。第一轮参数保留 20 px 死区和 800 脉冲/s 输出上限，初始参数为 `Kp=4.0`、`Ki=0.20`、`Kd=0.05`；随后针对实物到位抖动进行了第二轮调参，见下文。

- P 项：目标偏离画面中心越远，速度越大。
- I 项：累计持续偏差，补偿一直差一点的情况；积分值限制在 `±300 px*s`。
- D 项：根据相邻视觉帧误差变化率提前增加响应，并使用低通滤波抑制检测抖动。
- 目标丢失、进入死区、通信超时或人工停止时，清零该轴 PID 状态，避免下一次启动继承旧积分。
- 输出达到上限且误差仍推动同一方向时冻结积分，避免解除饱和后突然冲出。

这组参数已经通过主机控制器测试和 F407 源码语法检查；电机实物的跟随速度、振荡和突然加速响应仍需要下载后实测，不能提前写成最终调参结果。

## 到位抖动排查与第二轮调参（2026-08-24）

实测现象：目标已经接近画面中心后，云台仍在中心附近反复启停，摆动幅度明显。

排查结论：原控制器在误差进入死区时直接返回 0 脉冲/s；下一帧误差稍微越出死区后又立即按 PID 输出并启动 STEP。视觉检测有像素抖动，步进电机和机构又有惯性，因此形成“停止、反向、再停止”的循环。这个现象不等同于通信丢包，必须同时观察 `err_x/err_y`、`pan_speed/pitch_speed` 和 `pan_run/pitch_run`。

本轮修改：

- 中心死区从 20 px 放宽到 30 px，减少把视觉噪声当成有效偏差。
- 增加输出变化率限制，最大变化为 5000 脉冲/s²；进入中心区时速度逐帧下降到 0，离开中心区时也逐帧加速。
- 降低 `Kp` 和 `Ki`、提高 `Kd`，让靠近目标时更稳，同时保留远距离跟踪能力。
- PID 状态新增上一周期输出，用于计算软启动和软停止；目标丢失、人工停止等安全路径仍会立即清零。

### 代码动作 -> 工程意图

| 代码动作 | 工程意图 | 大白话描述给 AI |
| --- | --- | --- |
| `previous_output` | 记录上一帧速度目标 | “记住上一帧转多快，不要突然改变速度。” |
| `slew_output()` | 限制单位时间内的速度变化 | “让电机像有加减速曲线一样平滑启停。” |
| 中心区软停靠 | 进入死区后逐步降速 | “接近目标时慢慢刹车，避免冲过中心后反向抖动。” |

本轮先通过主机控制器测试，实物效果仍需重新编译、烧录后观察。验收时重点记录：目标静止时 `err_x/err_y` 是否在死区附近、`pan_speed/pitch_speed` 是否平滑收敛、`pan_run/pitch_run` 是否还频繁地 0/1 切换。
