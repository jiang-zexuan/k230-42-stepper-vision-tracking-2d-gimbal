# P10：F407 驱动 TFT 与触摸交互复盘

- 日期：2026-08-23
- 分支：`feature/f407-p10-dual-axis-visual-tracking`
- 阶段状态：TFT 纯色、FT6336G 触摸坐标和四区域交互已通过；正式中文状态仪表盘与中断化触摸待继续。
- 关联问题：`docs/08-debug/ISSUE-0011-tft-color-mismatch.md`

## 这一步做了什么

已经用 F407 的 SPI1 驱动 MSP2833/MSP2834 2.8 寸 TFT，建立了红、绿、蓝纯色验证。最初实测颜色异常，随后用 Logic 2 证明 F407 实际发出的命令、DC、CS 和 RGB565 字节正确。

已经对照模块用户手册、ILI9341 数据手册和官方 STM32 示例，移植完整初始化序列。重新下载后，屏幕已经正确显示红、绿、蓝，纯色诊断通过。

## 为什么要学

屏幕能点亮不等于屏幕已经正确工作。纯色测试把问题压缩成“指定颜色能否正确显示”，可以先验证供电、复位、背光、CS、DC、SPI 和控制器初始化，再进入文字和状态界面。

## 实测结果

| 项目 | 实际结果 | 状态 |
| --- | --- | --- |
| CubeMX/HAL 配置 | SPI1、8 位、MSB First、PB3/PB4/PB5 和 TFT GPIO 正确 | 已通过 |
| SPI 实际字节 | Logic 2 已确认命令和 RGB565 数据正确 | 已通过 |
| DC 命令/数据切换 | `0x3A` 时低，`0x55` 时高 | 已通过 |
| 最小初始化纯色 | 红、绿、蓝显示为蓝、紫、黄 | 已复现 |
| `0x20` 关闭反色 | 颜色仍异常 | 假设排除 |
| 低/高字节顺序实验 | 颜色映射改变但未恢复 | 不是根因 |
| 完整 ILI9341 初始化 | 红、绿、蓝显示正确 | 已通过 |
| TFT ID 读取 | `00 00 00 0F` | 不确定，不作为型号结论 |

## 代码动作 -> 工程意图

| 代码动作 | 工程意图 | 大白话描述给 AI |
| --- | --- | --- |
| `MX_SPI1_Init()` | 建立 F407 到 TFT 的 SPI 主机 | “让 F407 按指定 SPI 时序发送屏幕字节。” |
| `Tft_WriteCommand()` | 发送无参数控制命令 | “告诉屏幕执行某个操作。” |
| `Tft_WriteCommandData()` | 发送命令及参数 | “先告诉屏幕操作编号，再给它参数。” |
| 完整 `ILI9341_Init()` | 配置电源、Gamma、接口、像素格式和显示状态 | “先把屏幕控制器初始化到这个模块需要的工作状态。” |
| `ILI9341_FillColor()` | 写满整屏 RGB565 像素 | “先只验证颜色链路，不引入文字和界面变量。” |
| Logic 2 SPI 解码 | 观察线上真实字节 | “不要只相信代码，要证明引脚上真的发了这些数据。” |

## 复习要点

1. RGB565 颜色常量是标准值：`F800` 红、`07E0` 绿、`001F` 蓝。
2. 官方 STM32 示例的 16 位像素发送顺序是高字节先发、低字节后发。
3. “SPI 波形正确”只证明 F407 发得正确，不证明屏幕控制器已经完成正确初始化。
4. `TFT ID=00 00 00 0F` 不能直接证明型号错误；读回链路的可靠性需要模块资料保证。
5. 完整初始化序列是模块适配的一部分，不是可有可无的装饰代码。
6. 纯色诊断通过后，才能进入文字显示；先显示英文数字，再加入中文点阵，最后接入视觉状态。

## 触摸交互实测

已经完成以下链路：

1. 通过 `HAL_I2C_IsDeviceReady()` 确认 FT6336G 的 I2C 地址应答。
2. 通过 `HAL_I2C_Mem_Read()` 读取 `0x02` 和 `0x03~0x06`，获得触摸点数量与第一个触摸点原始坐标。
3. 实测当前显示为 180 度旋转，原始 X/Y 坐标均需反向：`screen_x=239-raw_x`、`screen_y=319-raw_y`。
4. 已实测四区域命中：左上、右上、左下、右下均能输出正确 `zone`。
5. 已实测触摸 UI 按下反色、松手恢复。
6. 已实测按钮按下沿动作：`MODE` 切换视觉跟踪、`TAKE` 切换接管、`STOP` 停止并清除位置有效性、`INFO` 读取电机状态。

### 代码动作 -> 工程意图

| 代码动作 | 工程意图 | 大白话描述给 AI |
| --- | --- | --- |
| `HAL_I2C_IsDeviceReady()` | 验证从设备 ACK | “先确认触摸芯片在这条总线上。” |
| `HAL_I2C_Mem_Read()` | 按寄存器读取触摸数据 | “先指定要读的寄存器，再取回数据。” |
| `screen_x=239-raw_x`、`screen_y=319-raw_y` | 适配显示 180 度旋转 | “把触摸芯片方向翻转成屏幕看到的方向。” |
| `touch_last_count==0` 到 `count==1` | 只响应按下沿 | “长按不要重复执行按钮命令。” |
| `STOP` 先停定时器再释放 EN | 建立安全停止顺序 | “先停止脉冲，再让电机可以自由转。” |

## 已问知识点与答案

- 为什么先轮询再中断：轮询便于先确认寄存器和坐标链路；正式版本再让 `CTP_INT` 只置位事件标志，I2C 读取仍放在主循环。
- 轮询是否浪费 MCU：当前 50 ms 一次、每次读取几个字节，对 F407 负载很小；中断化是后续降低空闲总线访问和提升响应的优化。
- 为什么 `chip_id=00` 不能直接判故障：`0xA0` 的默认值可能为 0，且当前识别寄存器读回值与资料默认值不一致，只能作为待验证诊断，不作为型号结论。

## 资料依据

- `D:\datasheet-knowledge\parsed\2.8inch_SPI_Module_MSP2833_MSP2834_User_Manual_CN\full.md` 第 3.1、3.2 节
- `D:\datasheet-knowledge\parsed\MSP2833_MSP2834_Specification_CN_V1.0\full.md`
- `D:\datasheet-knowledge\parsed\2.8inch_SPI_ILI9341_Demo_STM32\full.md` 的 `LCD_Init()` 与 `Lcd_WriteData_16Bit()`
- `D:\datasheet-knowledge\parsed\4-Driver_IC_Data_Sheet__ILI9341_Datasheet_part1\full.md` 第 8.2 节
